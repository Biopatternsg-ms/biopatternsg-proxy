version: '3.8'

services:
  nginx:
    build: .
    container_name: biopatternsg-proxy
    ports:
      - "80:80"   # Necesario para la redirección HTTP -> HTTPS
      - "443:443" # Tráfico seguro
    networks:
      - general-network

networks:
  general-network:
    external: true